import { PortfolioPieceItemQuery } from "../components/PortfolioListItem";

export const CuriosityPortfolioPieceItemQuery: PortfolioPieceItemQuery = {
  site: {
    siteMetadata: {
      siteUrl: "https://example.dev/"
    }
  }
};
